var __main = function () {
    // the following code can be replaced by a procedure for 
    // loading images from given path; to be implemented

    var g = Game.instance(function (game) {
        game.replaceScene(SceneTitle)
        game.run()
    })
}

__main()